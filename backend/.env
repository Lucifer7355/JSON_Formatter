# .env file

# The port the server will listen on
PORT=3000

# Whether to enable CORS for all origins (useful for development)
CORS_ORIGIN=*

# Environment (development/production)
NODE_ENV=development
